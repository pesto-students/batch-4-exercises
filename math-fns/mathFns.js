
function sqrt(value) {
  return Math.sqrt(value);
}

function power(value, pow) {
  return value ** pow;
}
function round(value) {
  return Math.round(value);
}

export {
  sqrt,
  power,
  round,
};
